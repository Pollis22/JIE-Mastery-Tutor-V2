# Agent3 FINAL FIX: Railway Production Login & Deployment

## Mission
Fix Railway production so the owner can log in with credentials: `pollis@mfhfoods.com` / `Crenshaw22$$`

## Root Causes Identified
1. ❌ Railway PostgreSQL database missing columns added yesterday (subscription_minutes_used, email_verified, etc.)
2. ❌ Password in Railway database doesn't match `Crenshaw22$$`
3. ✅ TypeScript errors already fixed by you
4. ❌ Code keeps crashing on Railway because it queries columns that don't exist

---

## Step 1: Add Missing Database Columns to Railway

Run this command to add ALL missing columns safely:

```bash
railway run psql $DATABASE_URL << 'EOF'
-- Add all missing columns with existence checks
DO $$ 
BEGIN
    -- Voice tracking columns
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='subscription_minutes_used') THEN
        ALTER TABLE users ADD COLUMN subscription_minutes_used INTEGER DEFAULT 0;
        RAISE NOTICE 'Added subscription_minutes_used';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='subscription_minutes_limit') THEN
        ALTER TABLE users ADD COLUMN subscription_minutes_limit INTEGER DEFAULT 60;
        RAISE NOTICE 'Added subscription_minutes_limit';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='purchased_minutes_balance') THEN
        ALTER TABLE users ADD COLUMN purchased_minutes_balance INTEGER DEFAULT 0;
        RAISE NOTICE 'Added purchased_minutes_balance';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='billing_cycle_start') THEN
        ALTER TABLE users ADD COLUMN billing_cycle_start TIMESTAMPTZ DEFAULT NOW();
        RAISE NOTICE 'Added billing_cycle_start';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='last_reset_at') THEN
        ALTER TABLE users ADD COLUMN last_reset_at TIMESTAMPTZ DEFAULT NOW();
        RAISE NOTICE 'Added last_reset_at';
    END IF;
    
    -- Auth columns
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verified') THEN
        ALTER TABLE users ADD COLUMN email_verified BOOLEAN DEFAULT true;
        RAISE NOTICE 'Added email_verified';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verified_at') THEN
        ALTER TABLE users ADD COLUMN email_verified_at TIMESTAMPTZ DEFAULT NOW();
        RAISE NOTICE 'Added email_verified_at';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verification_token') THEN
        ALTER TABLE users ADD COLUMN email_verification_token TEXT;
        RAISE NOTICE 'Added email_verification_token';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verification_expires') THEN
        ALTER TABLE users ADD COLUMN email_verification_expires TIMESTAMPTZ;
        RAISE NOTICE 'Added email_verification_expires';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='password_reset_token') THEN
        ALTER TABLE users ADD COLUMN password_reset_token TEXT;
        RAISE NOTICE 'Added password_reset_token';
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='password_reset_expires') THEN
        ALTER TABLE users ADD COLUMN password_reset_expires TIMESTAMPTZ;
        RAISE NOTICE 'Added password_reset_expires';
    END IF;
END $$;

-- Update existing users with safe defaults
UPDATE users 
SET 
    email_verified = COALESCE(email_verified, true),
    email_verified_at = COALESCE(email_verified_at, created_at, NOW()),
    subscription_minutes_used = COALESCE(subscription_minutes_used, 0),
    subscription_minutes_limit = COALESCE(subscription_minutes_limit, 60),
    purchased_minutes_balance = COALESCE(purchased_minutes_balance, 0),
    billing_cycle_start = COALESCE(billing_cycle_start, created_at, NOW()),
    last_reset_at = COALESCE(last_reset_at, created_at, NOW());

-- Show results
SELECT 'Migration Complete!' as status, COUNT(*) as total_users FROM users;
EOF
```

**Success criteria:** You should see output like:
```
NOTICE: Added subscription_minutes_used
NOTICE: Added subscription_minutes_limit
...
status: Migration Complete! | total_users: 1
```

---

## Step 2: Verify Columns Were Added

Run this to confirm all columns exist:

```bash
railway run psql $DATABASE_URL -c "
SELECT column_name, data_type, column_default 
FROM information_schema.columns 
WHERE table_name = 'users' 
  AND column_name IN (
    'subscription_minutes_used',
    'subscription_minutes_limit',
    'email_verified',
    'password_reset_token'
  )
ORDER BY column_name;
"
```

**Expected output:**
```
column_name                 | data_type | column_default
----------------------------|-----------|---------------
email_verified             | boolean   | true
password_reset_token       | text      | 
subscription_minutes_limit | integer   | 60
subscription_minutes_used  | integer   | 0
```

If you see all 4 columns, proceed to Step 3.

---

## Step 3: Check if Owner's Account Exists

```bash
railway run psql $DATABASE_URL -c "
SELECT 
    email,
    username,
    email_verified,
    subscription_minutes_limit,
    created_at
FROM users 
WHERE email = 'pollis@mfhfoods.com';
"
```

**If this returns a row:** Good! Account exists. Proceed to Step 4.

**If this returns nothing:** Account doesn't exist in Railway database. Create it:

```bash
railway run psql $DATABASE_URL << 'EOF'
INSERT INTO users (
    email,
    username,
    name,
    password,
    role,
    email_verified,
    subscription_minutes_limit,
    subscription_minutes_used,
    purchased_minutes_balance
) VALUES (
    'pollis@mfhfoods.com',
    'robbierobertson',
    'Robbie Robertson',
    'TEMPORARY_PASSWORD_WILL_BE_RESET',
    'admin',
    true,
    60,
    0,
    0
) ON CONFLICT (email) DO NOTHING;

SELECT 'Account created or already exists' as status;
EOF
```

---

## Step 4: Reset Password on Railway Database

Generate a proper scrypt hash and update the password:

```bash
# Step 4a: Generate password hash
node -e "
const crypto = require('crypto');
const { promisify } = require('util');
const scrypt = promisify(crypto.scrypt);

(async () => {
  const password = 'Crenshaw22\$\$';
  const salt = crypto.randomBytes(16).toString('hex');
  const buf = await scrypt(password, salt, 64);
  const hash = buf.toString('hex') + '.' + salt;
  
  console.log('Generated password hash for Crenshaw22\$\$');
  console.log('');
  console.log('Run this command:');
  console.log('');
  console.log('railway run psql \$DATABASE_URL -c \"UPDATE users SET password = \\'' + hash + '\\' WHERE email = \\'pollis@mfhfoods.com\\';\"');
})();
"
```

**Step 4b: Copy and run the UPDATE command** that was printed above. It will look like:
```bash
railway run psql $DATABASE_URL -c "UPDATE users SET password = 'abc123...xyz789' WHERE email = 'pollis@mfhfoods.com';"
```

**Step 4c: Verify password was updated:**
```bash
railway run psql $DATABASE_URL -c "
SELECT 
    email, 
    SUBSTRING(password, 1, 20) as password_preview,
    LENGTH(password) as password_length
FROM users 
WHERE email = 'pollis@mfhfoods.com';
"
```

**Expected output:**
```
email                | password_preview      | password_length
---------------------|----------------------|----------------
pollis@mfhfoods.com | a3f7b2c1d4e5f6abc123 | 161
```

If password_length is around 161 characters, it's correct.

---

## Step 5: Commit TypeScript Fixes and Deploy

```bash
# Commit all the TypeScript fixes you made
git add .
git commit -m "Fix TypeScript compilation errors and database schema"
git push origin main
```

Railway will auto-deploy. Watch the logs at: https://railway.app/project/YOUR_PROJECT/deployments

**Look for:**
```
✓ TypeScript compilation successful
✓ Build complete
✓ Starting server...
✓ Listening on 0.0.0.0:8080
✓ Database connection established
```

**Should NOT see:**
```
❌ ERROR: column "subscription_minutes_used" does not exist
❌ TypeScript compilation failed
```

---

## Step 6: Test Login

1. Go to: https://jie-mastery-tutor-v2-production.up.railway.app/auth
2. Email: `pollis@mfhfoods.com`
3. Password: `Crenshaw22$$`
4. Click "Sign In"

**Expected result:** ✅ Login successful, redirect to dashboard

---

## Step 7: Verify Railway Logs Show Success

Check Railway logs (https://railway.app/project/YOUR_PROJECT/logs):

**Should see:**
```
✅ [Auth] Login attempt for: pollis@mfhfoods.com
✅ [Auth] User found by email: YES
✅ [Auth] Password match: true
✅ [Auth] Login successful for: pollis@mfhfoods.com
```

**Should NOT see:**
```
❌ column "subscription_minutes_used" does not exist
❌ [Auth] User not found
❌ [Auth] Password match: false
❌ 500 Internal Server Error
```

---

## Troubleshooting

### If Step 1 fails with "railway: command not found"

Install Railway CLI:
```bash
npm install -g @railway/cli
railway login
railway link
```

Then retry Step 1.

### If Step 4 password generation fails

Use this simpler method:
```bash
railway run psql $DATABASE_URL -c "
UPDATE users 
SET password = (
    SELECT password 
    FROM users 
    WHERE email = 'pollis@mfhfoods.com' 
    LIMIT 1
)
WHERE email = 'pollis@mfhfoods.com';
"
```

Then try the forgot password flow on Railway to set a new password.

### If login still fails after all steps

Check these environment variables are set in Railway:
- DATABASE_URL (should be set automatically)
- SESSION_SECRET (needs to be a random 32+ character string)
- OPENAI_API_KEY (needs to start with sk-proj-)

---

## Success Criteria

After completing all steps:

✅ **Database migration succeeds** (Step 1 output shows "Migration Complete!")  
✅ **All columns exist** (Step 2 returns 4+ rows)  
✅ **Owner account exists** (Step 3 returns 1 row)  
✅ **Password is updated** (Step 4c shows ~161 character length)  
✅ **Railway deployment succeeds** (Step 5 shows no TypeScript errors)  
✅ **Login works** (Step 6 redirects to dashboard)  
✅ **Logs show success** (Step 7 shows "Login successful")  

---

## Alternative: If Railway Keeps Failing

If after all these steps Railway still doesn't work, use the working dev environment:

1. In Replit, click "Share" button
2. Make the Repl public
3. Get the public URL (ends with .repl.co)
4. Use that URL temporarily while we debug Railway further

The dev environment works perfectly - owner can log in there right now.

---

## Report Back

After running all steps, report:
1. Output from Step 1 (database migration)
2. Output from Step 2 (column verification)
3. Output from Step 3 (account check)
4. Whether login works in Step 6
5. Screenshot of Railway logs if login still fails

This will finally fix Railway login. The issue is 100% that the database schema doesn't match the code expectations.