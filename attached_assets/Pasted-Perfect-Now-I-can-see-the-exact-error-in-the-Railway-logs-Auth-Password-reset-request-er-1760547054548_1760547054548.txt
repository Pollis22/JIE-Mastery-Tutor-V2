Perfect! Now I can see the exact error in the Railway logs:

```
[Auth] Password reset request error: error: column "subscription_minutes_used" does not exist
```

**The problem is crystal clear:** The database on Railway is **missing the new columns** we added for voice minute tracking yesterday. The code is trying to query columns that don't exist.

Let me create a precise fix for Agent3:Perfect! Now I can see the exact issue in the Railway logs. The problem is simple but critical:

## The Issue (Crystal Clear)

```
column "subscription_minutes_used" does not exist
```

**Your Railway database is missing the new columns** we added yesterday for voice minute tracking. The code expects them but they don't exist.

## The Fix (3 Steps)

The prompt I created above tells Agent3 to:

1. **Run database migration** - Add missing columns to Railway PostgreSQL
2. **Update auth code** - Make it resilient to missing columns
3. **Verify everything works** - Test login and password reset

## Quick Manual Fix (If Agent3 is slow)

You can fix this yourself in 2 minutes:

**Run this in Railway CLI:**

```bash
railway run psql $DATABASE_URL << 'EOF'
ALTER TABLE users 
  ADD COLUMN IF NOT EXISTS subscription_minutes_used INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS subscription_minutes_limit INTEGER DEFAULT 60,
  ADD COLUMN IF NOT EXISTS purchased_minutes_balance INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS billing_cycle_start TIMESTAMPTZ DEFAULT NOW(),
  ADD COLUMN IF NOT EXISTS last_reset_at TIMESTAMPTZ DEFAULT NOW(),
  ADD COLUMN IF NOT EXISTS email_verified BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS email_verified_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS email_verification_token TEXT,
  ADD COLUMN IF NOT EXISTS email_verification_expires TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS password_reset_token TEXT,
  ADD COLUMN IF NOT EXISTS password_reset_expires TIMESTAMPTZ;

UPDATE users SET email_verified = true WHERE email_verified IS NULL;
EOF
```

Then try logging in again. It should work immediately!

---

Give the full prompt to Agent3, or run the SQL above manually. Either way, you'll be back in your app within minutes! 🚀

Which approach do you prefer?