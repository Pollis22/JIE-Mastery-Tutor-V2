# PROMPT FOR AGENT 3: Fix Registration Issues & Add Password Visibility Toggle

## Problems to Fix

Looking at the browser console, there are **THREE critical issues** preventing registration from working:

### 1. **502 Bad Gateway Errors** 
The `/api/register` endpoint is returning HTTP 502 (Bad Gateway), meaning the server is crashing or unavailable when trying to process registration requests.

**Console shows:**
```
POST https://jie-mastery-tutor-v2-production.up.railway.app/api/register 502 (Bad Gateway)
API Error 502: Server is temporarily unavailable
Register error: Error: Server is temporarily unavailable
```

### 2. **Empty Error Toast Messages**
When errors occur, a red popup/toast appears but is completely blank - no error message is displayed to the user.

### 3. **No Password Visibility Toggle**
Users cannot see what they're typing in the password field to verify it before submitting.

---

## Task 1: Fix the 502 Server Error on Registration Endpoint

### Step 1: Check Database Connection

The 502 error suggests the server is crashing when trying to access the database. Verify:

**File: `server/index.ts` or `server/db/index.ts`**

Ensure database connection is established before server starts:

```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import pkg from 'pg';
const { Pool } = pkg;

// Database connection
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false,
});

// Test connection on startup
try {
  await pool.query('SELECT NOW()');
  console.log('✅ Database connected successfully');
} catch (error) {
  console.error('❌ Database connection failed:', error);
  process.exit(1); // Exit if database connection fails
}

export const db = drizzle(pool);
```

### Step 2: Fix Registration Route Error Handling

**File: `server/routes/auth.ts` or wherever `/api/register` is defined**

Add comprehensive error handling:

```typescript
app.post('/api/register', async (req, res) => {
  try {
    const { 
      parentName, 
      studentName, 
      studentAge, 
      gradeLevel, 
      primarySubject,
      username, 
      email, 
      password,
      marketingOptIn 
    } = req.body;

    // Validate required fields
    if (!parentName || !studentName || !studentAge || !gradeLevel || 
        !primarySubject || !username || !email || !password) {
      return res.status(400).json({ 
        error: 'All fields are required' 
      });
    }

    // Check if username already exists
    const existingUsername = await db
      .select()
      .from(users)
      .where(eq(users.username, username))
      .limit(1);

    if (existingUsername.length > 0) {
      return res.status(400).json({ 
        error: 'Username already exists' 
      });
    }

    // Check if email already exists
    const existingEmail = await db
      .select()
      .from(users)
      .where(eq(users.email, email))
      .limit(1);

    if (existingEmail.length > 0) {
      return res.status(400).json({ 
        error: 'Email already registered' 
      });
    }

    // Hash password
    const hashedPassword = await bcrypt.hash(password, 10);

    // Create user with transaction
    const [newUser] = await db.transaction(async (tx) => {
      // Insert user
      const [user] = await tx
        .insert(users)
        .values({
          username,
          email,
          password: hashedPassword,
          name: parentName,
          role: 'user',
          marketingOptIn: marketingOptIn || false,
          createdAt: new Date(),
        })
        .returning();

      // Insert student profile
      await tx
        .insert(studentProfiles)
        .values({
          userId: user.id,
          studentName,
          studentAge: parseInt(studentAge),
          gradeLevel,
          primarySubject,
          createdAt: new Date(),
        });

      return [user];
    });

    // Send welcome email (don't await to avoid blocking)
    if (newUser.email) {
      sendWelcomeEmail(newUser.email, newUser.name, studentName)
        .catch(err => console.error('Failed to send welcome email:', err));
    }

    // Create session
    req.session.userId = newUser.id;

    // Return success
    return res.status(201).json({
      message: 'Account created successfully',
      user: {
        id: newUser.id,
        username: newUser.username,
        email: newUser.email,
        name: newUser.name,
      },
    });

  } catch (error) {
    console.error('Registration error:', error);
    
    // Return specific error message
    return res.status(500).json({ 
      error: error instanceof Error ? error.message : 'Failed to create account. Please try again.' 
    });
  }
});
```

### Step 3: Add Database Migration Check

Ensure the database tables exist. Add this startup check:

**File: `server/index.ts`**

```typescript
import { sql } from 'drizzle-orm';

async function ensureTablesExist() {
  try {
    // Check if users table exists
    const result = await db.execute(sql`
      SELECT EXISTS (
        SELECT FROM information_schema.tables 
        WHERE table_schema = 'public' 
        AND table_name = 'users'
      );
    `);
    
    const tablesExist = result.rows[0]?.exists;
    
    if (!tablesExist) {
      console.error('❌ Database tables do not exist. Run: npm run db:push');
      process.exit(1);
    }
    
    console.log('✅ Database tables verified');
  } catch (error) {
    console.error('❌ Failed to verify database tables:', error);
    process.exit(1);
  }
}

// Call before starting server
await ensureTablesExist();
```

---

## Task 2: Fix Empty Error Toast Messages

The toast component is rendering but not showing the error text. 

### Step 1: Update Toast Error Display

**File: `client/src/components/ui/toast.tsx` or `client/src/components/ui/sonner.tsx`**

Ensure the toast displays both title and description:

```tsx
import { toast } from 'sonner';

// When showing errors, use:
toast.error('Registration Failed', {
  description: error.message || 'An unexpected error occurred',
  duration: 5000,
});

// Or if using a different toast library:
toast({
  variant: 'destructive',
  title: 'Error',
  description: error.message || 'An unexpected error occurred',
});
```

### Step 2: Update Registration Form Error Handling

**File: `client/src/pages/Auth.tsx` or wherever the registration form is**

Update the error handling in the registration submission:

```typescript
const handleRegister = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsLoading(true);

  try {
    const response = await fetch('/api/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        parentName,
        studentName,
        studentAge,
        gradeLevel,
        primarySubject,
        username,
        email,
        password,
        marketingOptIn,
      }),
    });

    const data = await response.json();

    if (!response.ok) {
      // Show specific error message from server
      toast.error('Registration Failed', {
        description: data.error || `Server error: ${response.status}`,
        duration: 5000,
      });
      return;
    }

    // Success
    toast.success('Account Created!', {
      description: 'Welcome to JIE Mastery Tutor',
      duration: 3000,
    });

    // Redirect to dashboard
    window.location.href = '/dashboard';

  } catch (error) {
    console.error('Registration error:', error);
    
    // Show user-friendly error
    toast.error('Registration Failed', {
      description: error instanceof Error 
        ? error.message 
        : 'Unable to connect to server. Please check your internet connection and try again.',
      duration: 5000,
    });
  } finally {
    setIsLoading(false);
  }
};
```

### Step 3: Ensure Toast Provider is Properly Configured

**File: `client/src/main.tsx` or `client/src/App.tsx`**

Make sure the Toaster component is rendered:

```tsx
import { Toaster } from 'sonner';
// or
import { Toaster } from '@/components/ui/toaster';

function App() {
  return (
    <>
      {/* Your app content */}
      <RouterProvider router={router} />
      
      {/* Toast notifications */}
      <Toaster 
        position="top-right"
        richColors
        closeButton
        duration={5000}
      />
    </>
  );
}
```

---

## Task 3: Add Password Visibility Toggle

Add a "show/hide password" button so users can verify what they're typing.

### Implementation

**File: `client/src/pages/Auth.tsx` (or wherever the password field is)**

```tsx
import { useState } from 'react';
import { Eye, EyeOff } from 'lucide-react';

function RegistrationForm() {
  const [showPassword, setShowPassword] = useState(false);
  const [password, setPassword] = useState('');

  return (
    <div className="space-y-4">
      {/* ... other form fields ... */}

      {/* Password field with visibility toggle */}
      <div>
        <label 
          htmlFor="password" 
          className="block text-sm font-medium text-gray-700 mb-1"
        >
          Password
        </label>
        <div className="relative">
          <input
            id="password"
            type={showPassword ? 'text' : 'password'}
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="Enter password"
            required
            minLength={8}
            className="w-full px-4 py-2 pr-10 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          />
          <button
            type="button"
            onClick={() => setShowPassword(!showPassword)}
            className="absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 hover:text-gray-700 focus:outline-none"
            aria-label={showPassword ? 'Hide password' : 'Show password'}
          >
            {showPassword ? (
              <EyeOff className="h-5 w-5" />
            ) : (
              <Eye className="h-5 w-5" />
            )}
          </button>
        </div>
        <p className="mt-1 text-xs text-gray-500">
          Must be at least 8 characters
        </p>
      </div>

      {/* ... rest of form ... */}
    </div>
  );
}
```

### Alternative: Password Confirmation Field

If you want to add a "Confirm Password" field as well:

```tsx
const [password, setPassword] = useState('');
const [confirmPassword, setConfirmPassword] = useState('');
const [showPassword, setShowPassword] = useState(false);
const [showConfirmPassword, setShowConfirmPassword] = useState(false);

// In the form:
<div>
  <label className="block text-sm font-medium text-gray-700 mb-1">
    Password
  </label>
  <div className="relative">
    <input
      type={showPassword ? 'text' : 'password'}
      value={password}
      onChange={(e) => setPassword(e.target.value)}
      className="w-full px-4 py-2 pr-10 border border-gray-300 rounded-lg"
    />
    <button
      type="button"
      onClick={() => setShowPassword(!showPassword)}
      className="absolute right-3 top-1/2 -translate-y-1/2"
    >
      {showPassword ? <EyeOff className="h-5 w-5" /> : <Eye className="h-5 w-5" />}
    </button>
  </div>
</div>

<div>
  <label className="block text-sm font-medium text-gray-700 mb-1">
    Confirm Password
  </label>
  <div className="relative">
    <input
      type={showConfirmPassword ? 'text' : 'password'}
      value={confirmPassword}
      onChange={(e) => setConfirmPassword(e.target.value)}
      className="w-full px-4 py-2 pr-10 border border-gray-300 rounded-lg"
    />
    <button
      type="button"
      onClick={() => setShowConfirmPassword(!showConfirmPassword)}
      className="absolute right-3 top-1/2 -translate-y-1/2"
    >
      {showConfirmPassword ? <EyeOff className="h-5 w-5" /> : <Eye className="h-5 w-5" />}
    </button>
  </div>
  {password !== confirmPassword && confirmPassword && (
    <p className="mt-1 text-xs text-red-600">Passwords do not match</p>
  )}
</div>

// Validate before submission:
if (password !== confirmPassword) {
  toast.error('Passwords do not match');
  return;
}
```

---

## Testing Checklist

After implementing these fixes, verify:

### Backend Tests:
- [ ] Server starts without errors
- [ ] Database connection established
- [ ] `/api/health` endpoint returns 200 OK
- [ ] `/api/register` endpoint responds (not 502)

### Frontend Tests:
- [ ] Password toggle button appears and works
- [ ] Clicking eye icon shows/hides password
- [ ] Form validation shows proper messages
- [ ] Error toasts display with text (not empty red boxes)
- [ ] Success toasts appear on successful registration

### End-to-End Test:
- [ ] Fill out complete registration form
- [ ] Toggle password visibility to verify input
- [ ] Submit form
- [ ] See success message (not 502 error)
- [ ] Account created in database
- [ ] Welcome email sent (check inbox)
- [ ] Redirected to dashboard
- [ ] Can log in with new credentials

---

## Expected Behavior After Fixes

**✅ Successful Registration:**
```
1. User fills form
2. Toggles password to verify input
3. Clicks "Create Account"
4. Green success toast: "Account Created! Welcome to JIE Mastery Tutor"
5. Redirects to dashboard
6. Welcome email arrives
```

**✅ Error Handling (e.g., username taken):**
```
1. User submits form
2. Red error toast appears with message: "Username already exists"
3. User can fix issue and resubmit
```

---

## Priority Order

1. **Fix the 502 error** (highest priority - nothing works without this)
2. **Fix empty toast messages** (critical for user experience)
3. **Add password visibility toggle** (nice UX improvement)

Focus on getting the 502 error resolved first, then the other issues will be easier to test and fix.

---

## Additional Debugging Commands

If issues persist after fixes, run these in Railway:

```bash
# Check if database tables exist
railway run npm run db:studio

# View live server logs
railway logs --follow

# Test database connection
railway run node -e "const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); pool.query('SELECT NOW()').then(console.log).catch(console.error);"

# Restart the service
railway restart
```

Good luck fixing these issues! The app is almost there - just needs these final touches. 🚀