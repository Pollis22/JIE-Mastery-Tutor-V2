# Agent3: Final Fix - Path Mismatch & Simplify Flow

## Root Cause Found! 🎯

**Client calls:** `POST /api/session/realtime` → **404 Not Found**  
**Server has:** `POST /api/session/realtime/start` → Path mismatch!

Plus: Database table missing causes credential retrieval to fail.

## Solution: Single Unified Endpoint

Create ONE endpoint that does everything:
1. Creates OpenAI session
2. Gets ephemeral credentials
3. Returns them immediately
4. Saves to DB (fail-safe if table missing)

---

## Step 1: Replace/Update Session Route

**File: `server/src/routes/session.ts`**

```typescript
import { Router } from 'express';

const router = Router();

// POST /api/session/realtime - Single endpoint for everything
router.post('/session/realtime', async (req, res) => {
  const startTime = Date.now();
  
  try {
    console.log('🎬 [RealtimeAPI] Creating session via HTTP');
    
    const { 
      model = 'gpt-4o-realtime-preview', 
      voice = 'alloy',
      userId,
      studentId,
      subject,
      language,
      ageGroup 
    } = req.body;

    // Validate API key
    if (!process.env.OPENAI_API_KEY) {
      console.error('❌ Missing OPENAI_API_KEY');
      return res.status(500).json({ 
        error: 'Server configuration error' 
      });
    }

    // Request ephemeral session from OpenAI
    console.log('🔑 Requesting ephemeral session from OpenAI...');
    const openaiResponse = await fetch('https://api.openai.com/v1/realtime/sessions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ model, voice }),
    });

    if (!openaiResponse.ok) {
      const errorText = await openaiResponse.text();
      console.error('❌ OpenAI API error:', {
        status: openaiResponse.status,
        error: errorText
      });
      return res.status(openaiResponse.status).json({ 
        error: 'Failed to create OpenAI session',
        details: errorText 
      });
    }

    const sessionData = await openaiResponse.json();
    const sessionId = sessionData.id || `stub-${Date.now()}`;

    console.log('✅ OpenAI session created', { 
      sessionId,
      hasClientSecret: !!sessionData.client_secret?.value 
    });

    // Save to database (fail-safe, don't block on error)
    saveToDatabaseAsync({
      id: sessionId,
      userId,
      studentId,
      subject,
      language,
      ageGroup,
      voice,
      model,
      status: 'active',
      clientSecret: sessionData.client_secret?.value,
      expiresAt: sessionData.expires_at
    }).catch(err => {
      console.warn('⚠️ DB save failed (non-blocking):', err.message);
    });

    // Return credentials immediately
    const duration = Date.now() - startTime;
    console.log(`✅ [RealtimeAPI] Session ready in ${duration}ms`);

    res.json({
      success: true,
      sessionId,
      client_secret: sessionData.client_secret,
      model: sessionData.model,
      expires_at: sessionData.expires_at,
    });

  } catch (error) {
    console.error('❌ [RealtimeAPI] Error:', error);
    res.status(500).json({ 
      error: 'Internal server error',
      message: error.message 
    });
  }
});

// Async DB save with fail-safe for missing table
async function saveToDatabaseAsync(data: any) {
  try {
    // Your DB logic here (Prisma/Drizzle/raw SQL)
    // Example with raw SQL:
    /*
    await db.query(`
      INSERT INTO realtime_sessions (
        id, user_id, student_id, subject, language, 
        age_group, voice, model, status
      ) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
    `, [
      data.id, data.userId, data.studentId, data.subject,
      data.language, data.ageGroup, data.voice, data.model, data.status
    ]);
    */
    console.log('✅ Session saved to DB:', data.id);
  } catch (error: any) {
    // PostgreSQL error code 42P01 = "undefined_table"
    if (error.code === '42P01') {
      console.warn('⚠️ realtime_sessions table missing; skipping DB save');
      return; // Don't throw - this is expected
    }
    throw error; // Re-throw other errors
  }
}

export default router;
```

---

## Step 2: Ensure Route is Mounted

**File: `server/src/app.ts` or `server/src/index.ts`**

```typescript
import express from 'express';
import sessionRouter from './routes/session';

const app = express();

// Parse JSON
app.use(express.json({ limit: '25mb' }));

// Mount session routes - CRITICAL: Use /api prefix
app.use('/api', sessionRouter);

// Debug route to verify paths
app.get('/api/routes', (req, res) => {
  const routes = [];
  app._router.stack.forEach((middleware: any) => {
    if (middleware.route) {
      routes.push({
        path: middleware.route.path,
        methods: Object.keys(middleware.route.methods)
      });
    }
  });
  res.json({ routes });
});

// ... rest of your app

export default app;
```

**Test route registration:**
```bash
curl https://your-app.railway.app/api/routes | jq
```

Should include: `{"path": "/api/session/realtime", "methods": ["post"]}`

---

## Step 3: Verify Client Path (Should Already Be Correct)

**File: `client/src/hooks/use-realtime-voice.tsx`**

Make sure it's calling the correct path:

```typescript
const connect = async (config: any) => {
  try {
    console.log('🔑 [RealtimeVoice] Fetching WebRTC credentials via HTTP...');
    
    // This path should match the server route
    const response = await fetch('/api/session/realtime', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model: config.model || 'gpt-4o-realtime-preview',
        voice: config.voice || 'alloy',
        userId: config.userId,
        studentId: config.studentId,
        subject: config.subject,
        language: config.language,
        ageGroup: config.ageGroup,
      }),
    });

    console.log('📊 Response status:', response.status);

    if (!response.ok) {
      const errorData = await response.json().catch(() => ({}));
      console.error('❌ Request failed:', response.status, errorData);
      throw new Error(`Failed to get credentials: ${response.status} ${JSON.stringify(errorData)}`);
    }

    const data = await response.json();
    
    console.log('✅ Got credentials:', {
      sessionId: data.sessionId,
      hasSecret: !!data.client_secret?.value
    });

    if (!data.client_secret?.value) {
      throw new Error('No client_secret in response');
    }

    // Now establish WebRTC
    await connectWebRTC(data.client_secret.value, data.model);
    
  } catch (error) {
    console.error('❌ [RealtimeVoice] Connection failed:', error);
    throw error;
  }
};
```

---

## Step 4: Remove Old Routes (Cleanup)

**In your routes folder, find and remove/comment out:**

- `POST /api/session/realtime/start`
- `POST /api/session/realtime/:sessionId/credentials`

These are causing confusion. We only need ONE endpoint: `/api/session/realtime`

---

## Step 5: Create Database Table (Optional but Recommended)

**Run this SQL in Railway Postgres (Query tab):**

```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;

CREATE TABLE IF NOT EXISTS realtime_sessions (
  id TEXT PRIMARY KEY,
  user_id TEXT,
  student_id TEXT,
  subject TEXT,
  language TEXT,
  age_group TEXT,
  voice TEXT,
  model TEXT,
  status TEXT DEFAULT 'active',
  client_secret TEXT,
  expires_at TIMESTAMPTZ,
  started_at TIMESTAMPTZ DEFAULT NOW(),
  ended_at TIMESTAMPTZ,
  minutes_used NUMERIC(10,2) DEFAULT 0,
  error_message TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_rt_sessions_user ON realtime_sessions(user_id);
CREATE INDEX IF NOT EXISTS idx_rt_sessions_started ON realtime_sessions(started_at);

-- Verify it was created
SELECT COUNT(*) FROM realtime_sessions;
```

---

## Testing

### 1. Test the endpoint with curl:

```bash
curl -X POST https://jie-mastery-tutor-v2-production.up.railway.app/api/session/realtime \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-realtime-preview",
    "voice": "alloy",
    "userId": "test-user",
    "subject": "Math"
  }' \
  -v
```

**Expected response (200 OK):**
```json
{
  "success": true,
  "sessionId": "sess_...",
  "client_secret": {
    "value": "ek_...",
    "expires_at": 1234567890
  },
  "model": "gpt-4o-realtime-preview"
}
```

### 2. Check Railway Logs

Should see:
```
🎬 [RealtimeAPI] Creating session via HTTP
🔑 Requesting ephemeral session from OpenAI...
✅ OpenAI session created { sessionId: 'sess_...', hasClientSecret: true }
✅ [RealtimeAPI] Session ready in 123ms
```

If table exists:
```
✅ Session saved to DB: sess_...
```

If table missing (okay for now):
```
⚠️ realtime_sessions table missing; skipping DB save
```

### 3. Test in Browser

Click "Start Tutoring Session"

**Browser console should show:**
```
🔑 [RealtimeVoice] Fetching WebRTC credentials via HTTP...
📊 Response status: 200
✅ Got credentials: { sessionId: '...', hasSecret: true }
🔵 Creating RTCPeerConnection...
🎤 Requesting microphone access...
📤 Creating SDP offer...
🌐 Sending offer to OpenAI...
✅ WebRTC connection established
🎵 Received audio track from OpenAI
```

---

## Expected Flow (Success)

```
User clicks "Start Tutoring"
  ↓
[Client] POST /api/session/realtime
  ↓
[Server] Calls OpenAI API
  ↓
[Server] Returns credentials (200 OK)
  ↓
[Client] Establishes WebRTC to OpenAI
  ↓
[Client] Audio starts flowing
  ↓
✅ Success!
```

---

## Debugging If Still Failing

### If you get 404:

```bash
# Check what routes are registered
curl https://your-app.railway.app/api/routes

# Should include /api/session/realtime
```

### If you get 500:

```bash
# Check Railway logs for the actual error
railway logs --filter "RealtimeAPI"

# Common issues:
# - OPENAI_API_KEY not set
# - OpenAI API returned error
```

### If you get 200 but no client_secret:

```bash
# Check the response structure
curl -X POST https://your-app.railway.app/api/session/realtime \
  -H "Content-Type: application/json" \
  -d '{"voice":"alloy"}' | jq

# Should have: .client_secret.value
```

---

## Checklist

Before deploying:

✅ Remove old routes (`/api/session/realtime/start`, `/api/session/realtime/:id/credentials`)  
✅ Add new unified route (`POST /api/session/realtime`)  
✅ Mount router at `/api` prefix  
✅ Verify OPENAI_API_KEY is set in Railway  
✅ Test with curl before testing in browser  
✅ Check Railway logs show session creation  
✅ (Optional) Create database table to stop warnings  

---

## Success Criteria

✅ `curl` test returns 200 with `client_secret`  
✅ Railway logs: `✅ [RealtimeAPI] Session ready in Xms`  
✅ Browser console: `✅ Got credentials`  
✅ Browser console: `✅ WebRTC connection established`  
✅ Microphone activates and you can speak  
✅ You hear the AI tutor respond  
✅ No 404 errors  
✅ No connection loops  

This should get you across the finish line! 🎯