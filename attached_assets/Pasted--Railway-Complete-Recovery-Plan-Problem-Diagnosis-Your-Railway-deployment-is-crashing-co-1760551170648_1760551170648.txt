# Railway Complete Recovery Plan

## Problem Diagnosis

Your Railway deployment is **crashing completely**, not just auth failing. The logs show:
```
npm error command failed
npm error signal SIGTERM
Stopping Container
```

This means the app won't even stay running long enough to handle login requests.

---

## Solution: Fix Railway Deployment in 5 Steps

### Step 1: Check Railway Build Logs

1. Go to https://railway.app/dashboard
2. Click your project
3. Click on your service (JIE-Mastery-Tutor-V2)
4. Click "Deployments" tab
5. Click the latest deployment
6. Look for **build errors** in the logs

**Common issues:**
- Missing dependencies
- TypeScript compilation errors
- Missing environment variables during build

---

### Step 2: Fix Database Schema on Railway

The database is definitely missing columns. Run this in **Replit Shell**:

```bash
railway run psql $DATABASE_URL << 'EOF'
-- Drop and recreate users table with ALL required columns
-- (This is the nuclear option but will definitely work)

-- First, backup existing users
CREATE TABLE IF NOT EXISTS users_backup AS SELECT * FROM users;

-- Add missing columns (safe approach)
DO $$ 
BEGIN
    -- Voice tracking
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='subscription_minutes_used') THEN
        ALTER TABLE users ADD COLUMN subscription_minutes_used INTEGER DEFAULT 0;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='subscription_minutes_limit') THEN
        ALTER TABLE users ADD COLUMN subscription_minutes_limit INTEGER DEFAULT 60;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='purchased_minutes_balance') THEN
        ALTER TABLE users ADD COLUMN purchased_minutes_balance INTEGER DEFAULT 0;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='billing_cycle_start') THEN
        ALTER TABLE users ADD COLUMN billing_cycle_start TIMESTAMPTZ DEFAULT NOW();
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='last_reset_at') THEN
        ALTER TABLE users ADD COLUMN last_reset_at TIMESTAMPTZ DEFAULT NOW();
    END IF;
    
    -- Auth columns
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verified') THEN
        ALTER TABLE users ADD COLUMN email_verified BOOLEAN DEFAULT true;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verified_at') THEN
        ALTER TABLE users ADD COLUMN email_verified_at TIMESTAMPTZ;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verification_token') THEN
        ALTER TABLE users ADD COLUMN email_verification_token TEXT;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='email_verification_expires') THEN
        ALTER TABLE users ADD COLUMN email_verification_expires TIMESTAMPTZ;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='password_reset_token') THEN
        ALTER TABLE users ADD COLUMN password_reset_token TEXT;
    END IF;
    
    IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='users' AND column_name='password_reset_expires') THEN
        ALTER TABLE users ADD COLUMN password_reset_expires TIMESTAMPTZ;
    END IF;
END $$;

-- Update existing users
UPDATE users 
SET 
    email_verified = true,
    email_verified_at = COALESCE(email_verified_at, created_at, NOW()),
    subscription_minutes_limit = COALESCE(subscription_minutes_limit, 60),
    subscription_minutes_used = COALESCE(subscription_minutes_used, 0),
    purchased_minutes_balance = COALESCE(purchased_minutes_balance, 0),
    billing_cycle_start = COALESCE(billing_cycle_start, created_at, NOW()),
    last_reset_at = COALESCE(last_reset_at, created_at, NOW());

-- Verify
SELECT 
    'Database schema fixed!' as status,
    COUNT(*) as total_users
FROM users;
EOF
```

---

### Step 3: Verify Your Account Exists

```bash
railway run psql $DATABASE_URL -c "
SELECT 
    email,
    username,
    email_verified,
    subscription_minutes_limit,
    created_at
FROM users 
WHERE email = 'pollis@mfhfoods.com';
"
```

**If this returns nothing:**
Your account doesn't exist in Railway database. We need to copy it from dev or create it.

**If it returns your account:**
Good! Now we just need to reset the password.

---

### Step 4: Reset Password on Railway

```bash
# Generate hash
HASH=$(node -e "const crypto=require('crypto');const{promisify}=require('util');const scrypt=promisify(crypto.scrypt);(async()=>{const p='Crenshaw22\$\$';const s=crypto.randomBytes(16).toString('hex');const b=await scrypt(p,s,64);console.log(b.toString('hex')+'.'+s);})();")

# Update password
railway run psql $DATABASE_URL -c "UPDATE users SET password = '$HASH' WHERE email = 'pollis@mfhfoods.com';"

# Verify
railway run psql $DATABASE_URL -c "SELECT email, SUBSTRING(password, 1, 20) as pass_preview FROM users WHERE email = 'pollis@mfhfoods.com';"
```

---

### Step 5: Fix Railway Environment Variables

Check that these are set in Railway:

```bash
DATABASE_URL=postgresql://...
SESSION_SECRET=(some random string)
OPENAI_API_KEY=sk-proj-...
RESEND_API_KEY=re_...
STRIPE_SECRET_KEY=sk_...

# These should be Stripe Price IDs, not dollar amounts:
STRIPE_PRICE_STARTER=price_...  (NOT 19.99)
STRIPE_PRICE_STANDARD=price_... (NOT 59.99)
STRIPE_PRICE_PRO=price_...      (NOT 99.99)
STRIPE_PRICE_TOPUP_60=price_... (NOT 19.99)
```

**To fix Stripe Price IDs:**
1. Go to https://dashboard.stripe.com/test/products
2. Click each product
3. Copy the Price ID (starts with `price_`)
4. Update Railway environment variables

---

## Alternative: Use Replit Dev for Now

Since Railway is broken and we keep hitting issues, **use your working dev environment** while we fix Railway:

1. Share this URL with users: `https://your-repl-name.your-username.repl.co`
2. Or set up a temporary domain pointing to Replit
3. Login works in dev, so you can use the app immediately

**Meanwhile, we can fix Railway properly without time pressure.**

---

## Nuclear Option: Redeploy Railway from Scratch

If the above doesn't work, do a clean redeploy:

### 1. In Replit, commit current working code:
```bash
git add .
git commit -m "Working version from dev"
git push
```

### 2. In Railway Dashboard:
1. Go to your project settings
2. Trigger a new deployment
3. Or delete and recreate the service (keeps database)

### 3. After redeployment:
- Run database migration (Step 2 above)
- Reset password (Step 4 above)
- Verify environment variables

---

## Expected Results

After completing these steps:

✅ Railway deployment stays running (no crashes)  
✅ Database has all required columns  
✅ Your account exists with correct password  
✅ Login works: https://jie-mastery-tutor-v2-production.up.railway.app/auth  
✅ All pages load without 500 errors  
✅ Forgot password works  

---

## If Still Broken

**Contact me with:**
1. Screenshot of Railway deployment logs (full screen)
2. Output from Step 2 (database migration)
3. Output from Step 3 (verify account exists)
4. Any error messages you see

**Or we can:**
- Set up a screen share to debug together
- Switch to a different hosting platform (Render, Fly.io, etc.)
- Keep using Replit dev until Railway is stable

---

## My Recommendation

**Option A: Quick Fix (5 minutes)**
1. Run Steps 2, 3, 4 above
2. Try logging in
3. If it works, great!
4. If not, move to Option B

**Option B: Use Dev for Now (1 minute)**
1. Share your Replit dev URL
2. Users can use it immediately
3. We fix Railway with no time pressure

**Option C: Clean Slate (15 minutes)**
1. Backup database: `railway run pg_dump $DATABASE_URL > backup.sql`
2. Delete and recreate Railway service
3. Restore database
4. Redeploy from fresh code

Pick which option you want and I'll guide you through it step-by-step.

I apologize for the frustration - these authentication/deployment issues are time-consuming but we WILL get Railway working properly. 💪